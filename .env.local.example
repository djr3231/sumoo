# Google AI (Gemini)
# Get from: https://aistudio.google.com/apikey
GOOGLE_AI_KEY=AIza...

# Google OAuth (https://console.cloud.google.com → APIs & Services → Credentials)
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=...apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=...

# NextAuth
NEXTAUTH_SECRET=run "openssl rand -base64 32" and paste here
NEXTAUTH_URL=http://localhost:3000

# Optional: pin to a specific spreadsheet. If empty, the app creates one on first login.
SUMOO_SPREADSHEET_ID=

# Optional: Google Places API (New) key — used during dedup to clean up noisy
# OCR'd store names (e.g., "ג.מ. מעיין אלפיים (07) בע"מ" → "מעיין 2000").
# Google Cloud Console → enable "Places API (New)" → Credentials → Create API key.
# If empty, dedup runs without the Places verification step.
GOOGLE_PLACES_API_KEY=
